# contextBuilder
**Path:** `lib/copilot/contextBuilder.ts`
## Purpose

Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data.
## Exports

- **buildAuditContext** (function) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **AgentSignalsSnapshot** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **VerifiedInsightSnapshot** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **ChartSignalsSnapshot** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **StoreSummarySnapshot** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **BrandMetricsSnapshot** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **AuditContextInput** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi
- **AuditContext** (interface) — Context Builder — Assemble audit context for the Copilot from validated artifacts and SLM data. Gemini only receives thi

# modelSyncController
**Path:** `agents/modelSyncController.ts`
## Purpose

Model Sync Controller — synchronize SLM (charts/tables) + Gemini (narrative/insights) into unified PremiumState.
## Exports

- **syncModels** (function) — Model Sync Controller — synchronize SLM (charts/tables) + Gemini (narrative/insights) into unified PremiumState. If Gemi
- **SlmData** (interface) — Model Sync Controller — synchronize SLM (charts/tables) + Gemini (narrative/insights) into unified PremiumState. If Gemi
- **GeminiData** (interface) — Model Sync Controller — synchronize SLM (charts/tables) + Gemini (narrative/insights) into unified PremiumState. If Gemi

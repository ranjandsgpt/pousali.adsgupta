# analyticsEngineOutput
**Path:** `lib/analyticsEngineOutput.ts`
## Purpose

Step 13 — Analytics engine output format.
## Exports

- **createEmptyAnalyticsOutput** (function) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).
- **mergeMetricsIntoOutput** (function) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).
- **mergeValidationsIntoOutput** (function) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).
- **mergeForecastsIntoOutput** (function) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).
- **setInsightsInput** (function) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).
- **AnalyticsEngineOutput** (interface) — Step 13 — Analytics engine output format. Feeds the dual-engine insight pipeline (SLM + Gemini).

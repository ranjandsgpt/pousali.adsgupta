# geminiResponseValidation
**Path:** `lib/geminiResponseValidation.ts`
## Purpose

Response validation for Gemini outputs by mode.
## Exports

- **looksLikeJson** (function) — Response validation for Gemini outputs by mode. - Mode 2 (narrative): expect plain text; if JSON or code, extract or ret
- **looksLikeCode** (function) — Response validation for Gemini outputs by mode. - Mode 2 (narrative): expect plain text; if JSON or code, extract or ret
- **extractJsonObject** (function) — Response validation for Gemini outputs by mode. - Mode 2 (narrative): expect plain text; if JSON or code, extract or ret
- **validateNarrativeResponse** (function) — Response validation for Gemini outputs by mode. - Mode 2 (narrative): expect plain text; if JSON or code, extract or ret

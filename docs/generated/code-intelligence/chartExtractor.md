# chartExtractor
**Path:** `lib/chartExtractor.ts`
## Purpose

Chart Extractor — read chart metadata from UI / registry for export.
## Exports

- **extractChartsFromAuditData** (function) — Chart Extractor — read chart metadata from UI / registry for export. Extracts: chart type, axis labels, dataset, colors,
- **toChartSpec** (function) — Chart Extractor — read chart metadata from UI / registry for export. Extracts: chart type, axis labels, dataset, colors,
- **ExtractedChartMeta** (interface) — Chart Extractor — read chart metadata from UI / registry for export. Extracts: chart type, axis labels, dataset, colors,

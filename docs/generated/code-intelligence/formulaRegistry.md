# formulaRegistry
**Path:** `agents/formulaRegistry.ts`
## Purpose

Formula Registry — Human-readable formulas for transparency (aligned with amazonMetricsLibrary).
## Exports

- **getFormulaForMetric** (function) — Formula Registry — Human-readable formulas for transparency (aligned with amazonMetricsLibrary). Used when user asks "Ho
- **FormulaEntry** (interface) — Formula Registry — Human-readable formulas for transparency (aligned with amazonMetricsLibrary). Used when user asks "Ho

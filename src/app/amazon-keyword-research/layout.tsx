import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Amazon Keyword Research | Pousali Dasgupta",
  description: "Amazon keyword research strategy to identify high-converting search terms and improve product visibility and advertising performance.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

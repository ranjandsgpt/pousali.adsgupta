import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "How to Reduce Amazon ACOS | Pousali Dasgupta",
  description: "Learn how to reduce Amazon ACOS through campaign optimization, keyword strategy and bid management techniques.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
